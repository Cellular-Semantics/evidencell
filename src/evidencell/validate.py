# validate.py — stub for M1
# Wraps linkml-validate and OAK term validation.
# Currently invoked indirectly via `just validate` / `just validate-terms`.
# Will grow in M2+ to include snippet provenance checks.
